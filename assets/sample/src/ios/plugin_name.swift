import Foundation

@objc(plugin_name) class plugin_name: CDVPlugin  {

    //var callbackId: String!

function_name

    /**
     * sendSuccessCallback
     * @usage   used to success
     * */
    //private func sendSuccessCallback() {
    //    var message = Dictionary<String, Any>()
    //    message["result"] = true
    //    let pluginResult = CDVPluginResult.init(status: CDVCommandStatus_OK, messageAs: message)
    //    self.commandDelegate.send(pluginResult, callbackId: callbackId)
    //}

    /**
     * sendSuccessCallback
     * @usage   used to error with errorCode and errorMessage
     * */
    //private func sendFailCallback() {
    //    var message = Dictionary<String, Any>()
    //    message["result"] = false
    //    message["errorCode"] = 1
    //    message["errorMessage"] = "Error"
    //    let pluginResult = CDVPluginResult.init(status: CDVCommandStatus_ERROR, messageAs: message)
    //    self.commandDelegate.send(pluginResult, callbackId: callbackId)
    //}
}
